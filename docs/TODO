1. 当前 mermaid 代码渲染出了使用官方的开源的渲染器，
还增加了一些自定义的 DSL 语法来制定额外的样式，
但目前用户不知道支持哪些 DSL，需要用一个入口，
展示说明当前平台支持的自定义样式，然后再给出一版专门给 LLM 的提示词，
方便用户直接复制给第三方 AI，让 AI 编写 Mermaid 代码时能写出支持的自定义样式；

2. 使用导出 PNG 功能时，箭头连接线上的文字（S2 -->|"专家提交确认<br>invites.status → ACCEPTED"| SC["meeting_personnel<br>invite_status: CONFIRMED"]），导出后没有正常换行，为什么？svg 导出没有这个问题。

3. 使用 PNG / SVG 导出功能时，能否忽略当前的暗色主题（一个是网站设置的主题，还有一个是 mermaid 编辑器选择的主题），因为暗色主题总是有稀奇古怪的问题，直接使用亮色主题

4. 目前新建图表支持两种类型：默认的 MERMAID 和 HTML，我想新增一种类型：SVG，且支持用户 cmd+v 粘贴上传；

5. 切换网站主题色时，自动同步切换所有编辑器应用的主题色（基础/暗色），比如之前保存的 mermaid 图使用了暗色，但现在网站主题色切换为了浅色，mermaid 渲染器也应该
切换；进一步分析：我认为mermaid 图的主题色没必要存入数据库，不如改为运行时动态选择主题渲染

6. 新增功能：将 CodeEditor 内的代码中的 “\n“ 自动替换为"<br>"

7. 每个类型的图表，离开前都记下当时的缩放程度，回来时恢复到之前的缩放程度；

8. diagram-list-item 增加 tooltip 功能，鼠标悬停显示完整的 name